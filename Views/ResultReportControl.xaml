<UserControl x:Class="DescriptionFixer.Views.ResultReportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DescriptionFixer.Views"
             Width="600" Height="400">
    <Border Padding="10" Background="#FF1E1E2F" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Processing Completed" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#FF50FA7B"
                       Margin="0,0,0,10"/>

            <!-- Scrollable content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ReportStack" />
            </ScrollViewer>
            <!-- Close Button -->
            <Button Grid.Row="2" 
                    Content="Close" 
                    HorizontalAlignment="Stretch" 
                    Margin="10"
                    Click="CloseButton_Click"/>

        </Grid>
    </Border>
</UserControl>
