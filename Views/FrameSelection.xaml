<UserControl x:Class="DescriptionFixer.Views.FrameSelectionControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="600" Width="900">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
                    <Setter Property="Background" Value="#33007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox x:Name="ListBoxImages"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <DockPanel Grid.Row="1" Margin="0,10,0,0">
            <Label DockPanel.Dock="Left" FontWeight="Bold" Margin="10">Image Size:</Label>
            <Slider x:Name="SliderSize"
                    Width="400"
                    Margin="10"
                    Interval="10"
                    Maximum="900"
                    Minimum="150"
                    Value="250"
                    TickPlacement="BottomRight"
                    TickFrequency="25"
                    ValueChanged="SliderSize_ValueChanged"/>
            <Button Content="Select Image"
                    Name="ButtonSelectImage"
                    DockPanel.Dock="Bottom"
                    Margin="10"
                    Click="ButtonSelectImage_Click"/>
        </DockPanel>
    </Grid>
</UserControl>